// lista
const alunos = ['Yan', 'Savio']

// Adicionando itens na lista
alunos.push('Alex')

// Adicionando na posição exata ou substituir
alunos[3] = 'Binho'
console.log(alunos)

// Removendo o último e o primeiro item
alunos.pop()
 
alunos.shift()
console.log(alunos)
